# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

GET     /api/echo                       controllers.EchoController.index()
PATCH   /api/echo                       controllers.EchoController.index()
POST    /api/echo                       controllers.EchoController.index()
PUT     /api/echo                       controllers.EchoController.index()
DELETE  /api/echo                       controllers.EchoController.index()
HEAD    /api/echo                       controllers.EchoController.index()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)
